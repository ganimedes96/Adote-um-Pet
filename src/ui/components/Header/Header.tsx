import { HeaderContainer, Logo } from "./Header.style";

export const Header = () => {
    return(
        <HeaderContainer>
            <Logo src='images/logo.svg' alt='adote um pet'/>
        </HeaderContainer>
    )
}